const fs = require('node:fs');
const { join } = require('node:path');
const correct_path = join(__dirname, 'project-dist');
const html_path = join(correct_path,"index.html")
const css_path = join(correct_path,"style.css")

